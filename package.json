{
  "name": "dailypaper",
  "version": "2.0.0",
  "description": "DailyPaper - 自动文献汇总工具 (Cloudflare Workers版本)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test:fetch": "tsx src/test-local.ts",
    "test:server": "tsx test-local-server.ts",
    "test": "npm run test:fetch && npm run test:server"
  },
  "keywords": [
    "arxiv",
    "papers",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fast-xml-parser": "^4.3.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "tsx": "^4.7.0",
    "wrangler": "^3.50.0"
  }
}

