name = "dailypaper"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# KV 命名空间配置（用于存储论文数据）
[[kv_namespaces]]
binding = "PAPERS_KV"
id = ""  # 需要在 Cloudflare Dashboard 创建后填入

# 定时任务配置（每天 UTC 0:00 执行）
[triggers]
crons = ["0 0 * * *"]

# 环境变量
[vars]
ENVIRONMENT = "production"

